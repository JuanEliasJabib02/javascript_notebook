/**
 * 
 * What is SuperTest? 
 * Is a  library for testing node.js HTTP servers *.
 * It is a super-agent driven library for testing node.js HTTP servers 
 * using a fluent API. It provides a high-level abstraction for testing HTTP,
 *  while still allowing you to drop down to the lower-level API provided by superagent.
 */
