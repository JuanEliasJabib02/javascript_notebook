/**
 * Cors: 
 * 
 * Cross-origin resource sharing (CORS) is a mechanism that allow websites with
 * one URL to request data from another URL.
 * 
 * The browser implement the same-origin policy module as security that allow
 * the website to requests freely request images and data from its own
 * url but block anything from  an external URL
 */

/* In node.js use cors npm for enable it */