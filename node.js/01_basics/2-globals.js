/* 
  GLOBAL OBJECT IN NODE.JS - NO WINDOW

  When we work on node.js we dony have access to the windows object
  insteed we have acess to the global object that have a lot of
  bunch of useful things.

  In node.js is no window object, why? because there no browser.
 */

/**
 * So whats mean that concept "global variables"?,
 * What means a global variable is that
 * we can access to those variables no matter how complex are
 * our app.
 */


/* The most used global variables are */

// __dirname - path to current directory
// __filename - file name
// require -  function to use modules(CommonJS)
// module - info about current module(file)
// process - info about enviroment where the program is being executed
