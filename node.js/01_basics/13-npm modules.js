/**
 * What is NPM?
 * 
 * npm is the world's largest software registry. 
 * Open source developers from every continent use npm to share and borrow packages, 
 * and many organizations use npm to manage private development as well.
 */


/**
 * What is a package?
 * 
 * Is a folder that contains shared code (module)
 */

/* Npm come already instaled with node

npm --global --> comand
npm --version

Install local dependency
npm i <package name>
npm i <package name> -D --> Install as dev dependcy

Global dependcecy
npm install -g <packagename>


package.json - manifest file(stores important info about the project)

npm init -y (for start a package.json with all by default)

*/
