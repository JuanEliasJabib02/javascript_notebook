console.log("Hello from Node.js")

// Execute in the terminal node (filename).js

// Whats is the role of node .js in web developer

    /* RUN SERVER: Create server & Listen to incoming Requests */

    /* BUSSINESS LOGIC: Handle request, validate input, connect to database */

    /* RESPONSES : return responses(Rendered Json,htmls,files...) */
