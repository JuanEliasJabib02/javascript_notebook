/**
 * The foreach is a method using for iterate arrays
 * 
 * is executed at least at least once for each element
 */

// Example

const toDos = ["study", "code hideshi", "make beats"];

toDos.forEach((toDo) => {
  console.log(toDo)
})
