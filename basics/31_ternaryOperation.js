/*  The ternary operator take a 3 conditions,

    ? is exececuted if the condition is truthy
    : is executed if the condition is falsy
*/

const condition = true;
const ternaryOperator = condition ? true : false;
console.log(ternaryOperator)


//Example:

const aunthicated = false;

console.log(aunthicated ? "Is autenthicated" : "Get a subscription")