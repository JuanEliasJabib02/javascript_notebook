/**
* JavaScript string interpolation is the process 
* of embedding an expression into part of a string. 

*The string interpolation in JavaScript is performed
*by template literals (strings wrapped in backticks `)
*and ${expression} as a placeholder. For example:
*/

const NAME = "Juan";

console.log(`Hi my name is ${NAME}`);